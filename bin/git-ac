#!/usr/bin/env bash
#
# git ac <msg>
# stage everything and commit with the message
#

set -euo pipefail

if [ "$#" -lt 1 ] || [ -z "$1" ]; then
    echo "usage: git ac <msg>" >&2
    exit 1
fi

git add -A
git commit -m "$*"